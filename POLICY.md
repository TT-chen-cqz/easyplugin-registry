# 审核与下架规则（POLICY）

本文件写给维护者，也写给提交者——**提交前请先读完**，被驳回的多数是因为踩了这里的红线。

## 一、两道闸门

### 1. 自动校验（CI，合并前必须通过）

由 `scripts/validate_index.py` 与 `.github/workflows/validate.yml` 执行：

- 索引 JSON 结构与字段类型符合 `schema/index.schema.json`；
- `id` 唯一、按字母序排列，插件名是合法 Python 标识符；
- `version` 语义化且与 Release tag 一致；
- `repo` 形如 `owner/name`，`asset` 以 `.zip` 结尾；
- `sha256` 为 64 位小写十六进制。

### 2. 人工复核（维护者，按 CODEOWNERS）

机器查不出「代码到底干了什么」，所以合并前必须有人看一遍：

- [ ] 附件里的 `manifest.json` 与索引条目逐字段一致；
- [ ] 源码可读、无混淆（拼接字符串再 `exec` 一律驳回）；
- [ ] **声明的用途与实际行为相符**——例如声称「只在菜单加一条」，实际却发网络请求；
- [ ] 不修改 IDLE 自带文件，不写入 `site-packages` 之外的路径；
- [ ] 卸载路径干净：只清理自己的配置节与模块文件；
- [ ] 不调用外部可执行文件、不拉起后台进程、不打开监听端口；
- [ ] README 说明用途与用法，LICENSE 存在；
- [ ] 与已有条目的功能不重复（重复的话建议并入原项目）。

## 二、安全红线（一票否决）

以下行为**不接受**，无论用途多正当：

1. 未声明的网络访问（未在 `summary` 或 README 中明确说明的）；
2. `eval` / `exec` 执行来自运行时的字符串；
3. 调用 `subprocess` / `os.system` / `os.popen` 等启动外部命令；
4. 读写用户目录之外的路径、递归删除文件；
5. 隐藏行为：只在特定时间、特定机器、随机概率下触发的逻辑；
6. 收集并外传用户数据（代码内容、路径、环境变量等）；
7. 与声明用途无关的加密、混淆、打包可执行文件。

## 三、哈希与不可变性

- 索引里的 `sha256` 是「这个版本」的承诺，**客户端校验不通过就拒绝安装**；
- 作者修改已发布版本的内容必须**同时升版本号**，只换附件不升版本会被驳回；
- 建议（首次上架后逐步强制）作者仓库开启 **Immutable Releases**：
  开启后附件不可增删改、tag 不可移动，并带 Sigstore 格式的 release attestation；
- 维护者可用以下命令独立核验附件：

```bash
# 下载附件重算 sha256，与索引比对（不依赖 GitHub API 的签名校验）
python scripts/verify_release.py

# 仓库开启 Immutable Releases 后，可进一步核验验证声明
gh release verify v<版本号>
gh release verify-asset v<版本号> <附件名>
```

## 四、版本与兼容

- 最低客户端版本用 `min_client` 声明；低于该版本的客户端应拒绝安装并提示升级；
- 破坏性变更（改名、改配置节、改菜单结构）必须升主版本号；
- 条目 `id` 一旦上架**不许改**，改了等于新条目，老用户无法收到更新。

## 五、下架与撤回

| 情形 | 处理方式 |
|---|---|
| 作者主动下架 | 提 PR 把条目删掉；客户端不再展示，已装用户不受影响 |
| 发现安全问题 / 违反红线 | 把条目改为 `"yanked": true` 并填写 `"yank_reason"`，**保留条目** |
| 仓库归档、长期无人维护 | 先加 `yank_reason` 提示，90 天后删除条目 |
| 作者失联且存在严重漏洞 | 直接 yank，并在仓库 Issue 里公示原因 |

`yanked` 的语义：客户端**禁止新安装**，对已安装用户提示风险并建议卸载。
绝不静默删除——用户有权知道自己的插件为什么消失了。

## 六、维护者动作清单

1. 看 PR 的 CI 是否全绿；
2. 按上面第二、三节清单复核；
3. 用 `scripts/verify_release.py` 核一遍附件哈希；
4. 合并后把条目的 `verified` 改为 `true`（可放在同一个 PR 里，或合并后补一个 PR）；
5. `publish.yml` 会自动把 `index.json` 同步到 `store` 分支，客户端在下次拉取时看到。
